"use client";
import { ChevronDown, ChevronRight, FolderTree } from "lucide-react";
import { usePathname, useRouter } from "next/navigation";
import { useState, useTransition } from "react";
import { selectWorkspaceAction } from "@/app/dashboard/workspace-actions";
type Workspace = { id: string; name: string; role: string };
export function WorkspaceSwitcher({ workspaces, currentId }: { workspaces: Workspace[]; currentId: string }) { const router = useRouter(); const pathname = usePathname(); const [open, setOpen] = useState(true); const [pending, start] = useTransition(); const [error, setError] = useState<string | null>(null); const active = workspaces.some((workspace) => pathname === `/workspace/${workspace.id}`); function choose(id: string) { if (id === currentId && pathname === `/workspace/${id}`) return; setError(null); start(async () => { try { await selectWorkspaceAction(id); if (pathname === "/wallets") router.refresh(); else router.push(`/workspace/${id}`); } catch { setError("Không thể chuyển workspace."); } }); } return <div className="workspace-tree"><button type="button" className={`nav-item workspace-tree-trigger ${active ? "nav-item-active" : ""}`} onClick={() => setOpen((value) => !value)} aria-expanded={open} aria-controls="workspace-tree-list"><span className="flex items-center gap-2"><FolderTree size={17}/><span className="workspace-tree-title">Workspace</span></span><span className="workspace-tree-meta flex items-center gap-2"><small>{workspaces.length}</small>{open ? <ChevronDown size={16}/> : <ChevronRight size={16}/>}</span></button>{open && <div id="workspace-tree-list" className="workspace-tree-list" aria-label="Workspace bạn được phép hoạt động">{workspaces.map((workspace) => <button type="button" key={workspace.id} disabled={pending} onClick={() => choose(workspace.id)} className={`workspace-tree-item ${pathname === `/workspace/${workspace.id}` ? "workspace-tree-current" : ""}`}><span className="workspace-tree-branch" aria-hidden="true"/><span className="min-w-0 flex-1 truncate text-left">{workspace.name}</span><small>{workspace.role === "ADMIN" ? "Admin" : "Member"}</small></button>)}{error && <p className="workspace-tree-error">{error}</p>}</div>}</div>; }
