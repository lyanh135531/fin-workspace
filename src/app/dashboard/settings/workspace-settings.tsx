"use client";
import { Settings2 } from "lucide-react";
import { useState, useTransition } from "react";
import { updateWorkspaceSettingsAction } from "@/app/dashboard/settings/actions";
type Workspace = { name: string; description: string | null; baseCurrency: string; timeZone: string; approvalRequired: boolean; status: "active" | "deactive" };
export function WorkspaceSettings({ workspace, isAdmin }: { workspace: Workspace; isAdmin: boolean }) { const [message, setMessage] = useState<string | null>(null); const [pending, start] = useTransition(); function save(form: FormData) { start(async () => { const result = await updateWorkspaceSettingsAction({ name: form.get("name"), description: form.get("description") || undefined, baseCurrency: "VND", timeZone: "Asia/Ho_Chi_Minh", approvalRequired: form.get("approvalRequired") === "on", status: form.get("status") }); setMessage(result.ok ? "Đã lưu cấu hình workspace." : result.message); }); } if (!isAdmin) return <section className="sunrise-card settings-readonly-card"><p className="settings-eyebrow">Cấu hình workspace</p><h2>Bạn đang xem với quyền thành viên</h2><p>Chỉ Admin có thể thay đổi cấu hình của workspace này.</p></section>; return <section className="sunrise-card settings-config-card"><div className="settings-section-icon"><Settings2 size={18}/></div><div className="settings-section-heading"><p className="settings-eyebrow">Cấu hình workspace</p><h2>Thiết lập cơ bản</h2><p>Thông tin nhận diện và cơ chế phê duyệt giao dịch.</p></div><form action={save} className="settings-config-form"><label>Tên workspace<input className="field" required name="name" defaultValue={workspace.name}/></label><label>Trạng thái<select name="status" defaultValue={workspace.status} className="field"><option value="active">Đang hoạt động</option><option value="deactive">Tạm ngưng</option></select></label><label className="settings-wide-field">Mô tả<textarea className="field settings-textarea" name="description" defaultValue={workspace.description ?? ""} placeholder="Mô tả ngắn về mục đích của workspace"/></label><label className="settings-toggle"><input name="approvalRequired" type="checkbox" defaultChecked={workspace.approvalRequired}/><span><strong>Yêu cầu duyệt giao dịch</strong><small>Giao dịch mới chờ Admin phê duyệt trước khi làm thay đổi số dư.</small></span></label><div className="settings-fixed-values"><span>Tiền tệ <strong>{workspace.baseCurrency}</strong></span><span>Múi giờ <strong>{workspace.timeZone}</strong></span></div><div className="settings-form-footer"><p role="status">{message}</p><button disabled={pending} className="button-primary">{pending ? "Đang lưu" : "Lưu thay đổi"}</button></div></form></section>; }
