"use client";
import { Check, Copy, KeyRound } from "lucide-react";
import { useState } from "react";
export function InviteCodeCard({ code }: { code: string }) { const [copied, setCopied] = useState(false); async function copy() { await navigator.clipboard.writeText(code); setCopied(true); setTimeout(() => setCopied(false), 1500); } return <section className="sunrise-card settings-invite-card"><div className="settings-section-icon"><KeyRound size={18}/></div><div><p className="settings-eyebrow">Chỉ dành cho Admin</p><h2>Mã mời workspace</h2><p className="settings-card-copy">Người nhận dùng mã để gửi yêu cầu tham gia; bạn duyệt yêu cầu đó trong thông báo.</p></div><div className="invite-code-row"><code>{code}</code><button onClick={copy} className="button-secondary icon-button" title={copied ? "Đã sao chép" : "Sao chép mã mời"} aria-label={copied ? "Đã sao chép mã mời" : "Sao chép mã mời"}>{copied ? <Check size={17}/> : <Copy size={17}/>}</button></div></section>; }
